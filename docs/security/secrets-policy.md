# Secrets Policy

Never commit passwords, JWT private keys, API tokens, MinIO secrets, RabbitMQ credentials, database credentials, or integration tokens.

Commit only safe placeholders such as `.env.example` and `secrets/README.md`.
