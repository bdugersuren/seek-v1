# Gemini Prompt — Repository Initialisation

Read `GEMINI.md` and all relevant `.agents` documents.

Do not implement business features.

Prepare a Mongolian execution plan for:

- pnpm monorepo;
- `portal-web`;
- `assessment-web`;
- sixteen service folders;
- shared packages;
- root `docker-compose.yml`;
- `docker-compose.dev.yml`;
- profiles;
- external `shared-net`;
- PostgreSQL, Redis, RabbitMQ, MinIO;
- health endpoints;
- service documentation templates;
- CI quality gates.

Before modifying files, list every proposed file and ask for explicit approval.
