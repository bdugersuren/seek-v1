{
  "name": "seek-platform",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "build": "pnpm --recursive build"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
