import React from "react";

/**
 * QueryProvider Placeholder
 * Ирээдүйд TanStack Query (@tanstack/react-query) нэмэгдэхэд
 * энд тохируулж, Compose хийх боломжтой байдлаар бэлдэв.
 */
export function QueryProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
