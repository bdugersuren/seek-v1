import {mapToQuiz} from '../apps/portal-web/src/features/assessor-workspace/api';
test('Quiz uses selected revision metadata and does not invent runtime policy',()=>{const q:any=mapToQuiz({id:'q',title:'Old',templateId:'bp',selectedRevision:{id:'r',title:'New',revisionStatus:'PUBLISHED',passingScore:'0',durationMinutes:45,maxAttempts:3,totalMaxScore:'8',runtimePolicy:{questionOverrides:[]}}});expect(q.title).toBe('New');expect(q.status).toBe('published');expect(q.passingScore).toBe(0);expect(q.durationMinutes).toBe(45);expect(q.accessMode).toBeUndefined();expect(q.resultReleaseMode).toBeUndefined()});
